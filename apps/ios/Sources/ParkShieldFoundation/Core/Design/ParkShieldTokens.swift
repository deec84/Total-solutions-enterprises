import SwiftUI

enum ParkShieldTokens {
    static let brand = Color(red: 21 / 255, green: 94 / 255, blue: 239 / 255)
    static let surface = Color.white
    static let onSurface = Color(red: 16 / 255, green: 24 / 255, blue: 40 / 255)
    static let danger = Color(red: 180 / 255, green: 35 / 255, blue: 24 / 255)

    enum Spacing {
        static let extraSmall: CGFloat = 4
        static let small: CGFloat = 8
        static let medium: CGFloat = 16
        static let large: CGFloat = 24
    }
}
